import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';
import New from './new';

ReactDOM.render(
  <React.StrictMode>
  <New/>
  </React.StrictMode>,
  document.getElementById('root')
);
